{
  "name": "lucazini.it",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel index.html",
    "build": "npx parcel build index.html --public-url https://maguroguro.github.io/lucazini.it/",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.10.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.3",
    "gh-pages": "^6.1.0"
  }
}
